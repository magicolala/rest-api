AppBundle\Entity\User:
    exclusion_policy: none
    properties:
        id:
            groups: ["user", "preference", "auth-token"]
        firstname:
            groups: ["user", "preference", "auth-token"]
        lastname:
            groups: ["user", "preference", "auth-token"]
        email:
            groups: ["user", "preference", "auth-token"]
        preferences:
            groups: ["user"]
